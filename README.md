# Env-watch
The App Purpose:
Continuously ping important services (HTTP endpoints, TCP ports, maybe Kubernetes services), 
measure uptime and latency, keep history in a DB, and expose an API to see current status and SLO compliance.
